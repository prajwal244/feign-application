
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/feignclient
    username: root
    password: test
  sql:
    init:
      platform: mysql
  jpa:
    hibernate:
      ddl-auto: create
  application:
    name: PRODCUT-DETAILS
      
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
server:
  port: 8888
 

